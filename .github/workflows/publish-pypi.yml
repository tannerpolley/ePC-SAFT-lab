name: publish-to-pypi

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to publish, usually a tag such as v0.2.0"
        required: true
        default: "v0.2.0"
        type: string

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ inputs.ref }}
  cancel-in-progress: false

jobs:
  pypi-preflight:
    name: PyPI trusted publisher preflight
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}

      - name: Check PyPI project and release version
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import json
          import os
          import sys
          import tomllib
          import urllib.error
          import urllib.request

          project = "epcsaft"
          repository = os.environ.get("GITHUB_REPOSITORY", "ePC-SAFT/ePC-SAFT")
          version = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))["project"]["version"]
          url = f"https://pypi.org/pypi/{project}/json"
          try:
              with urllib.request.urlopen(url, timeout=30) as response:
                  payload = json.loads(response.read().decode("utf-8"))
          except urllib.error.HTTPError as exc:
              if exc.code == 404:
                  summary = Path(os.environ["GITHUB_STEP_SUMMARY"])
                  summary.write_text(
                      "## PyPI first-publish preflight\n\n"
                      "PyPI does not have a published `epcsaft` project yet. "
                      "This is valid only when PyPI has a matching pending publisher "
                      f"for `{repository}`, workflow `publish-pypi.yml`, "
                      "environment `pypi`.\n",
                      encoding="utf-8",
                  )
                  print("PyPI project does not exist yet; continuing for trusted-publisher first publish.")
                  sys.exit(0)
              raise
          releases = payload.get("releases", {})
          if version in releases:
              raise SystemExit(f"PyPI already has {project} {version}; bump the version before publishing.")
          print(f"PyPI project exists and {project} {version} is not published yet.")
          PY

  build-sdist:
    name: build sdist
    runs-on: windows-latest
    needs: [pypi-preflight]

    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install 3.13

      - name: Build sdist
        run: uv build --sdist

      - name: Upload sdist
        uses: actions/upload-artifact@v7.0.1
        with:
          name: pypi-sdist
          path: dist/*.tar.gz

  build-wheels:
    name: build Windows wheel
    runs-on: ${{ matrix.os }}
    needs: [pypi-preflight]
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]

    steps:
      - uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install 3.13

      - name: Sync dependencies
        run: uv sync --locked --no-install-project

      - name: Build wheels
        run: uv run --locked cibuildwheel --output-dir wheelhouse
        env:
          CIBW_BUILD: "cp313-*"
          CIBW_ARCHS_WINDOWS: "AMD64"
          CIBW_SKIP: "*-win32 *-musllinux*"
          CIBW_TEST_REQUIRES: "numpy"
          CIBW_TEST_COMMAND: "python -c \"import numpy as np; from epcsaft import Mixture, ParameterSet, State; p=ParameterSet.from_dict({'MW': np.asarray([16.043e-3]), 'm': np.asarray([1.0]), 's': np.asarray([3.7039]), 'e': np.asarray([150.03])}, species=['Methane']); s=State(Mixture(p), T=300.0, x=np.asarray([1.0]), rho=100.0); print(s.z())\""

      - name: Upload wheels
        uses: actions/upload-artifact@v7.0.1
        with:
          name: pypi-wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

  publish:
    name: publish to PyPI
    runs-on: ubuntu-latest
    needs: [build-sdist, build-wheels]
    environment:
      name: pypi
      url: https://pypi.org/project/epcsaft/
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Download distributions
        uses: actions/download-artifact@v8.0.1
        with:
          pattern: pypi-*
          path: dist
          merge-multiple: true

      - name: Show distributions
        run: ls -lh dist

      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
