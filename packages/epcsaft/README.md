# epcsaft

Provider package for the ePC-SAFT equation of state.

This package owns the public `epcsaft` distribution, provider Python API,
provider-native `_core` module, provider native SDK metadata, and provider
package tests inside the ePC-SAFT monorepo.
