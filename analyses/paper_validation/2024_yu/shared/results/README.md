# Results

This scaffold intentionally contains no generated results. Future results should be written by `scripts/run_all.py` only after source tables are normalized and public package native Ipopt solves are accepted.

